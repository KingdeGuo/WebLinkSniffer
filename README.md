# 网址获取浏览器插件

一个用于获取当前网页中所有超链接的浏览器插件，支持分页展示、缓存管理和批量打开功能。

## 功能特性

- 🔍 **自动获取链接**：自动扫描当前网页中的所有超链接
- 📄 **分页展示**：每页显示5个链接，支持上一页/下一页导航
- 🗂️ **智能排序**：自动将已打开的链接移到列表末尾
- 💾 **缓存管理**：记住已打开的链接，避免重复打开
- 🚀 **批量打开**：一键打开当前页的所有链接（在新标签页中）
- ⚙️ **自定义设置**：可配置是否自动移动已打开的链接

## 文件结构

```
网址获取/
├── manifest.json      # 插件配置文件
├── popup.html        # 弹出页面HTML
├── popup.css         # 弹出页面样式
├── popup.js          # 弹出页面逻辑
├── content.js        # 内容脚本（获取页面链接）
├── test.html         # 测试页面
└── README.md         # 说明文档
```

## 安装方法

### Chrome 浏览器

1. 打开 Chrome 浏览器，进入扩展程序页面：
   - 地址栏输入：`chrome://extensions/`
   - 或点击菜单 → 更多工具 → 扩展程序

2. 开启右上角的 **"开发者模式"**

3. 点击 **"加载已解压的扩展程序"** 按钮

4. 选择本插件的文件夹（包含所有上述文件）

5. 插件安装完成，浏览器工具栏会显示插件图标

### Edge 浏览器

1. 打开 Edge 浏览器，进入扩展程序页面：
   - 地址栏输入：`edge://extensions/`

2. 开启左下角的 **"开发人员模式"**

3. 点击 **"加载解压缩的扩展"** 按钮

4. 选择本插件的文件夹

## 使用方法

1. **打开插件**：
   - 点击浏览器工具栏中的插件图标
   - 弹出窗口会显示当前页面的所有链接

2. **浏览链接**：
   - 每页显示5个链接
   - 使用"上一页"/"下一页"按钮导航
   - 已打开的链接会显示为灰色

3. **管理链接**：
   - 勾选链接旁边的复选框标记为已打开
   - 取消勾选可移除标记

4. **批量操作**：
   - 点击"刷新链接"重新获取当前页面链接
   - 点击"打开本页链接"批量打开当前页的所有链接

5. **设置选项**：
   - 勾选"自动将已打开的链接移到末尾"启用智能排序

## 技术实现

### 核心组件

1. **content.js** - 内容脚本
   - 注入到每个网页中
   - 获取所有 `<a>` 标签的链接
   - 过滤无效链接（JavaScript、邮件、锚点等）
   - 去重和排序

2. **popup.js** - 弹出页面逻辑
   - 管理链接列表和分页
   - 处理用户交互（打开链接、标记已读）
   - 使用 Chrome Storage API 缓存数据
   - 与内容脚本通信获取链接

3. **缓存机制**
   - 使用 `chrome.storage.local` 存储已打开的链接
   - 支持跨会话持久化
   - 可配置的自动排序功能

### 兼容性

- Chrome 88+ (Manifest V3)
- Edge 88+
- 其他基于 Chromium 的浏览器

## 测试方法

1. 打开 `test.html` 文件（本地测试页面）
2. 点击插件图标查看获取到的链接
3. 测试分页、标记、批量打开等功能

## 注意事项

- 插件需要"activeTab"权限来获取当前标签页内容
- 需要"storage"权限来保存用户设置和缓存
- 不会收集或上传任何用户数据
- 所有数据仅存储在本地浏览器中

## 自定义开发

如需修改插件功能：

1. **修改分页大小**：在 `popup.js` 中修改 `pageSize` 变量
2. **调整样式**：修改 `popup.css` 文件
3. **更改链接过滤规则**：修改 `content.js` 中的 `getAllLinks()` 函数
4. **添加新功能**：扩展 `popup.js` 中的 `LinkManager` 类

## 故障排除

**问题：插件无法获取链接**
- 确保当前页面已完全加载
- 刷新页面后重试
- 检查浏览器控制台是否有错误

**问题：链接显示不全**
- 某些动态加载的链接可能需要页面交互后才能获取
- 尝试滚动页面或点击"刷新链接"按钮

**问题：插件图标不显示**
- 重新加载已解压的扩展程序
- 检查 manifest.json 文件格式是否正确

## 许可证

本项目仅供学习和演示使用，可自由修改和分发。

## 更新日志

### v1.0 (2024-03-08)
- 初始版本发布
- 基本链接获取功能
- 分页展示和缓存管理
- 批量打开链接功能